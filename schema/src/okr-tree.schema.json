{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://okr-agents.dev/schema/v1/okr-tree.schema.json",
  "title": "OkrTree",
  "description": "The whole OKR tree as a flat node list. Structure lives in the parent pointers; uniqueness, parent existence and acyclicity are checked separately by checkTreeSemantics().",
  "type": "object",
  "properties": {
    "nodes": {
      "type": "array",
      "description": "Every node in the tree, in no particular order.",
      "items": {
        "$ref": "okr-node.schema.json",
        "description": "One OKR node."
      },
      "maxItems": 500
    }
  },
  "required": ["nodes"],
  "additionalProperties": false
}
